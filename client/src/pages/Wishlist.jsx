import { Link } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
export default function Wishlist({ wishlist, addToCart, toggleWishlist }) { return <main className="shop"><div className="shop-title"><small>SAVED FOR LATER</small><h1>Wishlist</h1><p>{wishlist.length ? `${wishlist.length} saved piece${wishlist.length > 1 ? 's' : ''}` : 'Your wishlist is waiting for something special.'}</p></div>{wishlist.length ? <div className="product-grid shop-grid">{wishlist.map((p) => <ProductCard key={p.id} product={p} addToCart={addToCart} toggleWishlist={toggleWishlist} isWishlisted />)}</div> : <div className="empty-state"><h2>Nothing saved yet</h2><p>Tap the heart on any piece to keep it here.</p><Link className="primary-btn" to="/shop">Explore collection</Link></div>}</main>; }
